# Several transitive deps (gatsby, superstatic via firebase-tools) pin
# "node <= 22" in their engines field; CI runs Node 22 but local machines may
# be newer. The pins are advisory — skip the check so installs (including the
# nested one isolate-package runs during functions deploy) succeed locally.
--ignore-engines true
