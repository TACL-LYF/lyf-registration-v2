{
  "firestore": [
    {
      "database": "lyf-v2",
      "rules": "firestore.rules",
      "indexes": "firestore.indexes.json"
    },
    {
      "database": "lyf-v2-test",
      "rules": "firestore.rules",
      "indexes": "firestore.indexes.json"
    }
  ],
  "functions": [
    {
      "source": "functions/isolate",
      "codebase": "v2",
      "ignore": [
        "node_modules",
        ".git",
        "firebase-debug.log",
        "firebase-debug.*.log"
      ],
      "predeploy": [
        "yarn workspace lyf-registration-schemas build",
        "yarn --cwd functions run lint",
        "yarn --cwd functions run build",
        "yarn --cwd functions run isolate",
        "cp functions/.env functions/isolate/.env"
      ],
      "runtime": "nodejs22"
    }
  ],
  "emulators": {
    "functions": {
      "port": 5001
    },
    "auth": {
      "port": 9099
    },
    "ui": {
      "enabled": true
    },
    "singleProjectMode": true,
    "firestore": {
      "port": 8080
    }
  }
}
