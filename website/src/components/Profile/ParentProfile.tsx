import React from "react"

import {Parent} from "@utils/databaseSchema"
import { Card } from "@mui/material"

type ParentProfileProps = {
  parent: Parent
}

export default function ParentProfile({parent}: ParentProfileProps) {
  return (
    <Card>

    </Card>
  )
}
