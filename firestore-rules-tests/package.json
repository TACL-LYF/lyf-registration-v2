{
  "name": "firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Security rules tests for firestore.rules — run against the Firestore emulator",
  "scripts": {
    "test": "jest --runInBand",
    "test:emulator": "cd .. && firebase emulators:exec --only firestore --project demo-lyf-rules \"yarn workspace firestore-rules-tests test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "@types/jest": "^29.5.14",
    "firebase": "^10.12.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "~5.8.3"
  }
}
